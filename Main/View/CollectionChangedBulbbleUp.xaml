<UserControl x:Class="Main.CollectionChangedBulbbleUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Main"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    <DataGrid  ItemsSource="{Binding Trans}" AutoGenerateColumns="False" HorizontalAlignment="Left" VerticalAlignment="Top">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Id" Binding="{Binding TransactionId}" CanUserReorder="True"/>
        <DataGridTextColumn Header="EntryDate" Binding="{Binding TransactionDesc}" CanUserReorder="True"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
